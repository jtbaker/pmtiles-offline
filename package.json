{
  "name": "pmtiles-offline",
  "version": "1.0.0",
  "description": "Use PMTiles offline in mapping applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "tsx test/offline.test.ts",
    "example:maplibre": "cd examples/maplibre_offline && esbuild maplibre_offline.ts --bundle --outfile=./maplibre_offline.js --sourcemap --serve=8000 --servedir=."
  },
  "repository": {
    "type": "git",
    "url": "github.com/jtbaker/pmtiles-offline"
  },
  "keywords": [
    "pmtiles",
    "offline",
    "maps"
  ],
  "author": "Jason Baker",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/node": "^25.0.3",
    "esbuild": "^0.27.2",
    "fake-indexeddb": "^6.2.5",
    "maplibre-gl": "^5.15.0",
    "pmtiles": "^4.3.1",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0"
  }
}
